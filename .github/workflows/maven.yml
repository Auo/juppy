
name: maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: GitHub Action for Maven
      uses: LucaFeger/action-maven-cli@1.1.0
      args:
        - 'clean install'
