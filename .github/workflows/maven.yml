
name: run-maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    steps:
    - uses: LucaFeger/action-maven-cli@1.1.0
    - name: GitHub Action for Maven
